#include <stdio.h>
#include <conio.h>
#include<stdlib.h>
int main()
{
    int a;
    printf("Enter the number:");
    scanf("%d",&a);
    printf("Absolute value=%d",abs(a));
    getch();
    return 0;
}